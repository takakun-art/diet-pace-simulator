# diet-pace-simulator

減量ペース・シミュレーター（単一HTML `index.html`。身長・体重・体脂肪率から、減量ペース別に「いつ目標に届くか」と「そのとき除脂肪体重がどれだけ削れるか」を同時に出す。計算用の依存ライブラリなし・入力値は送信しない。外部通信はアクセス計測スクリプト1本のみ＝下記「アクセス計測」参照）。

**公開URL**: https://takakun-art.github.io/diet-pace-simulator/

## この道具の狙い

**誰がどの瞬間に触るか**：ダイエットを始めようとして「いつまでに何キロ落とせるのか」「速く落とすのは何がまずいのか」が分からない瞬間。

世の中の計算ツールは「1日◯kcal」という**数字を1個返して終わる**。このツールは**速さと引き換えに何を失うか**を並べて出すのが本体（表の右2列＝「目標まで」と「失う筋肉」がトレードオフになっている）。ここが「人が動くポイント」だという仮説で作っている。

提案P-013「小型プロダクト実験場」の**2本目**（1本目は `pokemon-type-matrix/quiz.html`）。

## 健康に関わる内容なので、変更時に必ず守ること

- **医療・栄養の助言として書かない。** 冒頭のディスクレーマーを消さない・弱めない
- **安全側の警告を外さない**。現在4つある:
  1. 摂取カロリーが基礎代謝を下回る行がある
  2. 摂取カロリーが下限（男性1,500 / 女性1,200 kcal）を下回る行がある
  3. 現在のBMIが18.5未満
  4. 目標体脂肪率が必須脂肪に近い（男性8%未満 / 女性15%未満）
- **「失う筋肉」の推定は幅のある前提**であることを `<details>` 内で明示している。断定に書き換えない
- 数字を変えたら、必ず**端の条件**（低BMI・低体脂肪率・女性・高齢）で検算してから公開する

## 計算式

| 項目 | 式 |
|---|---|
| 除脂肪体重 LBM | 体重 ×(1 − 体脂肪率) |
| 基礎代謝 | Katch-McArdle: 370 + 21.6 × LBM |
| 維持カロリー | 基礎代謝 × 活動係数（1.2 / 1.375 / 1.55 / 1.725） |
| 脂肪1kg | 7,200 kcal |
| ペース | 週あたり体重比 0.25 / 0.5 / 0.75 / 1.0 / 1.5 % |
| 減量分のうち除脂肪組織の割合 | 順に 5 / 5 / 15 / 25 / 35 %（推定・幅あり） |

体重が減ると維持カロリーも下がるため、**毎週再計算**して推移を出している（直線にはならない）。

## 検算の記録（2026-08-05）

37歳/170cm/75kg/体脂肪25% → 目標15%（活動1.375）で：

| ペース | 摂取 | 目標まで | 失う筋肉 |
|---|---|---|---|
| ゆっくり | 1,987 | 54週 | −0.5kg |
| ふつう | 1,794 | 27週 | −0.5kg |
| 速い | 1,601 | 21週 | −1.6kg |
| かなり速い | 1,408 | 19週 | −3.3kg |
| 無茶 | 1,022 | 15週 | −5.3kg |

女性30歳/160cm/60kg/30%→22%、痩せ型25歳/175cm/60kg/12%→10% でも検算済み（警告が正しく出ることを確認）。

## アクセス計測（2026-08-11 設置）

`index.html` の `<head>` に GoatCounter のスニペットがある。**`window.GC_CODE` が `"MYCODE"` の
間は外部スクリプトを一切読み込まない**（＝計測は動かない・通信もしない）。実コードに書き換えると動き出す。

- **体重・体脂肪率・年齢などの入力値は絶対に送らない。** 「入力を触った」という事実だけを
  `sim-used` として1セッション1回だけ送る。CLAUDE.md冒頭の「入力値は送信しない」は維持されている
- 同じスニペットが `pokemon-type-matrix/quiz.html` にもある。**片方を直したらもう片方も揃える**
- 2作品とも `takakun-art.github.io` 配下なので、GoatCounterのサイト登録は**1つでよい**（パスで区別）
- 計測が壊れてもシミュレーター本体は動く（`gcEvent` は未ロード時に黙って捨てる）
- 記録と週次報告は `~/.claude/scheduled-tasks/product-traffic-weekly/SKILL.md` が担当

## 運用ルール
- Git運用は `~/.claude/CLAUDE.md` の「Git 運用ルール」に従う（ファイルを作成・更新したら必ずコミット。コミット本文に判断者〈くんたか / Claude〉と変更理由を書く）。
